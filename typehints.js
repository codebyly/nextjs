//@ts-check

/**
 * multpliziert a und b // beschreibung
 * @param {number} a
 * @param {number} b
 * @returnd {number}
 */
function multiply(a, b) {
  return a * b;
}

// multiply(2, "D");
multiply(2, 3);
